# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration
MONGODB_URI=mongodb://localhost:27017/waitlist

# Authentication
JWT_SECRET=your_super_secure_jwt_secret_key_here_change_this_in_production
API_KEY=your_environment_api_key_for_admin_operations

# Security Note:
# - JWT_SECRET: Used for JWT token generation (if needed in future)
# - API_KEY: Environment-level API key for administrative operations
#   This key allows full access to create projects, view all waitlists, etc.
#   Keep this secure and only use server-side!

# Project API tokens are auto-generated and stored in the database
# Each project gets its own unique API token for frontend integration
